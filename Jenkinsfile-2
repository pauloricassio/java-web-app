pipeline {
  agent { label 'linux' }
  options {
    buildDiscarder(logRotator(numToKeepStr: '5'))
  }
  stages {
    stage('Build') {
      steps {
        sh 'docker build -t pauloricassio/java-web-app:latest .'
      }
    }
    stage('Scan') {
      steps {
        sh 'trivy --exit-code 1 --severity HIGH,CRITICAL pauloricassio/java-web-app:latest'
      }
    }
  }
}
